import ComingSoon from '../components/ui/ComingSoon';

const Schedule = () => (
  <ComingSoon
    title="Schedule"
    blurb="The two-day event schedule will be posted here once dates are locked in."
  />
);

export default Schedule;
